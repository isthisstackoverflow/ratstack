/**
 * Enum that describes how making a thing is restricted.
 * It may be unrestricted (forever producible),
 * faction-restricted (can be made once per faction),
 * region-restricted (can be made once per region).
 */
export const RESTRICTION = {
  NONE: 0,
  FACTION: 1,
  REGION: 2
}
