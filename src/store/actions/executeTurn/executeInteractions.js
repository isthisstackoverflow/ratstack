export default factionIndex => state => async dispatch => {
  // user can shift rats around to
  // * steal
  // * terrorize (if researched)
  // * subjugate (if researched)
  // * move (if neighbouring region is own)
  // each rat may move once, keep track

  // user can decide what to build per region, shows estimated time (if think for that is given)
  // user can decide what to research globally, shows estimated time (if think for that is given)

  // everything is executed after plan is set on pressing a button
}
